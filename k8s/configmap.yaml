apiVersion: v1
kind: ConfigMap
metadata:
  name: employee-app-config
  namespace: employee-management
  labels:
    app.kubernetes.io/name: employee-management
data:
  DB_HOST: mysql
  DB_NAME: employeedb
